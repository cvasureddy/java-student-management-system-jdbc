# java-student-management-system-jdbc
Enterprise-level Student Management System built using Java and JDBC with MySQL integration. Implements full CRUD operations, PreparedStatement-based secure queries, transaction management, connection pooling structure, batch processing, stored procedure support, and modular layered architecture (Config–DAO–Service–Util–Report).
